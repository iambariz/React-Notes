import { createTheme } from "@material-ui/core/styles";

const noteTheme = createTheme({
	palette: {
		primary: {
			main: "#ffc400;",
			light: "#ffce2c",
			dark: "#bb8f00",
		},
		secondary: {},
	},
});

export default noteTheme;
